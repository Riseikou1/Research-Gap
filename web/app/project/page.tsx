import {siteContent}from"@/lib/content";export default function Project(){return <div className="page"><p className="eyebrow">Method & project</p><h1>Inspectability is the product.</h1><p>Research GAP preserves the original idea, deterministic query baseline, generated query provenance, retrieval routes, normalized papers, structured evidence, landscape patterns, verification queries, counterexamples, provider failures, and coverage limitations.</p><div className="limitation"><strong>Scientific boundary</strong><p>{siteContent.limitation}</p></div></div>}
